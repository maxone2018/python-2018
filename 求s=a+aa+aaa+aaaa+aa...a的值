# 题目：求s=a+aa+aaa+aaaa+aa...a的值，
# 其中a是一个数字。例如2+22+222+2222+22222(此时共有5个数相加)，几个数相加由键盘控制。
# 程序分析：关键是计算出每一项的值。

num = input('请输入任意一个1~9的整数：')
how = int(input('请输入你想的的循环相加的次数：'))
all_add = 0
for i in range(1, how+1):
    all_add += int(num * i)   #用到字符串乘以一个数就是重复多少次的特性

print('结果是：', all_add)
