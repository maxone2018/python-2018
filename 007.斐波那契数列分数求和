# 题目：有一分数序列：2/1，3/2，5/3，8/5，13/8，21/13...求出这个数列的前20项之和。
# 程序分析：请抓住分子与分母的变化规律。

# 分子的规律是第三个数是前面两个数的和，第一个数从2开始
# 分母的规律也是第三个数是前面两个数的和，第一个数从1开始
# 这就是一个斐波那契数列规律



def division(m=1, n=2, step=1, sum=0.0):
    if step <= 20:
        sum += float(n / m)    #由于这里sum = sum +...  所以必须将sum定义成全局变量
        step += 1
        print('前{}项和是：'.format(step-1), sum)
        return division(n, m + n, step, sum)

division()
